package org.krista.seabattle.test;

import org.junit.Test;
import org.krista.seabattle.BattleShip;
import org.krista.seabattle.Coordinate;

import javax.json.Json;
import javax.json.JsonArrayBuilder;

import java.util.Arrays;

import static org.assertj.core.api.Assertions.*;

/**
 * TO-DO
 */
public class TestJsonCreator {

    @Test
    public void addInfoAboutDestroyedShipTest() {
        /*
        JsonArrayBuilder jsonTest = Json.createArrayBuilder();
        BattleShip shipTest = new BattleShip(Arrays.asList(new Coordinate(0,0),new Coordinate(0,1)));
        */
        assertThat(true).isTrue();

    }
}
